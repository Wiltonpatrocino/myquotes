import React from 'react';

const Quote = (quotes) => {

    return (
        <div>
            <p>
                {quotes[index].quote}
            </p>
        </div>
    );
};

export default Quote;